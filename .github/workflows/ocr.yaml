name: OCR Service Disabled

on:
  workflow_call:
    secrets:
      SSH_PRIVATE_KEY:
        required: true
      SERVER_USER:
        required: true
      SERVER_ADDR:
        required: true
      ALIYUN_USERNAME:
        required: true
      ALIYUN_PASSWORD:
        required: true

jobs:
  ocr_disabled:
    runs-on: ubuntu-latest
    steps:
      - name: OCR deployment disabled
        run: echo "OCR service is kept in the repository but is no longer built or deployed."
